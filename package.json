{
  "name": "runar-ts",
  "private": true,
  "version": "0.0.1",
  "packageManager": "bun@1.1.24",
  "workspaces": [
    "runar-ts-*"
  ],
  "scripts": {
    "build": "bash -lc 'set -euo pipefail; for d in runar-ts-*; do echo Building $d; (cd "$d" && bun run build); done'",
    "clean": "bash -lc 'set -euo pipefail; for d in runar-ts-*; do echo Cleaning $d; (cd "$d" && bun run clean || true); done'"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.5.4"
  }
}


