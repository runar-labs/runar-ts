{
  "name": "runar-ts-decorators",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "cd generator && bun run generate-types.ts && cd .. && tsc -p tsconfig.json && cd test_fixtures && tsc",
    "clean": "rimraf dist && rimraf test_fixtures/dist",
    "test": "bun test",
    "generate-types": "cd generator && bun run generate-types.ts",
    "build-with-types": "bun run generate-types && tsc -p tsconfig.json",
    "build-fixtures": "cd test_fixtures && tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.4",
    "rimraf": "^6.0.1"
  }
}
